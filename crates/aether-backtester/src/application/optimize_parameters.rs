//! Placeholder for parameter sweeps / optimization (future work).

use aether_common::AetherResult;

pub struct OptimizeParameters;

impl OptimizeParameters {
    pub fn placeholder() -> AetherResult<()> {
        Ok(())
    }
}
