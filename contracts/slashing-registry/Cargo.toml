[package]
name = "aether-contract-slashing-registry"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Slashing registry message types (CosmWasm-ready)"

[lib]
crate-type = ["lib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
