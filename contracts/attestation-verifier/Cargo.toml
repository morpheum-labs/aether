[package]
name = "aether-contract-attestation-verifier"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Attestation verification message types (CosmWasm-ready)"

[lib]
crate-type = ["lib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
