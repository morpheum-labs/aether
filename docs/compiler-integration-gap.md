# Compiler ↔ Aether gap (pointer)

The **detailed gap review and checklist** lives in the compiler repo:

**`agentscript-compiler/docs/aether-integration-gap.md`**  
In a typical sibling checkout: `../agentscript-compiler/docs/aether-integration-gap.md`.

Guest ABI contract: [`agentscript-guest-abi.md`](agentscript-guest-abi.md).
